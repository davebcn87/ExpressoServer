<h1>Listing extra_disponibles</h1>

<table>
  <tr>
    <th>Beguda</th>
    <th>Extra</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @extra_disponibles.each do |extra_disponible| %>
  <tr>
    <td><%= extra_disponible.beguda_id %></td>
    <td><%= extra_disponible.extra_id %></td>
    <td><%= link_to 'Show', extra_disponible %></td>
    <td><%= link_to 'Edit', edit_extra_disponible_path(extra_disponible) %></td>
    <td><%= link_to 'Destroy', extra_disponible, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Extra disponible', new_extra_disponible_path %>
