<h1>Listing usuaris</h1>

<table>
  <tr>
    <th>Nom</th>
    <th>Pass</th>
    <th>Saldo</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @usuaris.each do |usuari| %>
  <tr>
    <td><%= usuari.nom %></td>
    <td><%= usuari.pass %></td>
    <td><%= usuari.saldo %></td>
    <td><%= link_to 'Show', usuari %></td>
    <td><%= link_to 'Edit', edit_usuari_path(usuari) %></td>
    <td><%= link_to 'Destroy', usuari, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Usuari', new_usuari_path %>
