<%= form_for(@usuari) do |f| %>
  <% if @usuari.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usuari.errors.count, "error") %> prohibited this usuari from being saved:</h2>

      <ul>
      <% @usuari.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br />
    <%= f.text_field :nom %>
  </div>
  <div class="field">
    <%= f.label :pass %><br />
    <%= f.text_field :pass %>
  </div>
  <div class="field">
    <%= f.label :saldo %><br />
    <%= f.text_field :saldo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
